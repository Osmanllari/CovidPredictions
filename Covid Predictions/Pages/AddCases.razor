@*@page "/cases/add"
@page "/cases/add/{casesId:int}"

@using Covid_Predictions.Data;

<PageTitle>History of Predictions</PageTitle>

<h1 style="color: white">History of Predictions</h1>

<a href="#" class="btn btn-outline-dark">Show All</a>
<div class="row">
    <div class="col-md-6">
        <EditForm>
            <div class="form-group">
                <label for="location">Location</label>
                <InputText class="form-control" @bind-Value="cases.location" />
                <ValidationMessage For="@(()=>cases.location)" />
            </div>

            <div class="form-group">
                <label for="iso_code">Iso Code</label>
                <InputText class="form-control" @bind-Value="cases.iso_code" />
                <ValidationMessage For="@(()=>cases.iso_code)" />
            </div>

            <div class="m-2">
                @message
            </div>

            <div class="m-2">
                <button type="submit" class="btn btn-primary">Save</button>
            </div>
        </EditForm>
    </div>
</div>


@code {
    [Parameter]
    public int CasesId { get; set; }
    private string message = string.Empty;
    Cases cases = new();

    private void Save()
    {

    }
}
*@